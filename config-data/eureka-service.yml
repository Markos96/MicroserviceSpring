server:
    port: 8761
eureka:
    client:
        fetch-regitry: false
        register-with-eureka: false
        service-url:
            default-zone: http://${eureka.instance.hostname}:${server.port}/eureka/
